# RAG 系统模式指南

## 概述

RAG 系统现在支持两种工作模式，让你在"严格"和"灵活"之间选择，以解决知识库相关性不足时的问题。

## 两种模式对比

### 📋 严格模式 (Strict Mode)

**特点**：仅基于知识库回答，信息不足时明确说明

**何时使用**：
- 需要完全符合知识库内容
- 不希望模型使用先验知识
- 法律/合规性要求严格

**示例**：
```
用户问题：JavaScript 中的 Promises 是什么？
知识库：无相关内容

系统回答：
知识库中没有相关信息
```

**启用方式**：
```bash
# 命令行
uv run python -m src.cli --rag-mode strict

# 或在配置文件中
# config/model_config.yaml
rag:
  mode: "strict"
```

---

### 🚀 灵活模式 (Flexible Mode) - 推荐

**特点**：优先使用知识库，不足时补充先验知识

**何时使用**：
- 希望系统更智能、更有用
- 知识库是参考而非唯一来源
- 追求答案的完整性

**示例**：
```
用户问题：JavaScript 中的 Promises 是什么？
知识库：在回调函数笔记中有简短提及

系统回答：
[知识库] 根据我的笔记，Promises 用于处理异步操作...
[补充] 此外，Promises 通常与 async/await 一起使用...

置信度: 0.50 🟠 低
RAG模式: 灵活模式（知识库优先+补充）
检索统计: 1 个文档
```

**启用方式**：
```bash
# 命令行（推荐）
uv run python -m src.cli --rag-mode flexible

# 或在配置文件中（默认）
# config/model_config.yaml
rag:
  mode: "flexible"
```

---

## 置信度指标解释

系统会自动根据检索质量计算置信度：

| 置信度 | 标签 | 颜色 | 含义 |
|--------|------|------|------|
| 0.95 | 极高 | 🟢 | 5+ 个高分文档 (>0.7)，完全基于知识库 |
| 0.85 | 高 | 🟢 | 3-5 个中等文档 (>0.5)，可靠的知识库答案 |
| 0.70 | 中等 | 🟡 | 1-3 个相关文档，基本覆盖 |
| 0.50 | 低 | 🟠 | 无高质量文档，基于先验知识补充 |
| 0.20 | 极低 | 🔴 | 无相关文档且无法补充 |

**解读**：
- 🟢 极高/高：充分信任答案，基本来自知识库
- 🟡 中等：答案可用但不完美
- 🟠 低：答案包含先验知识，需要验证
- 🔴 极低：答案不可靠，建议自己查证

---

## 配置文件设置

编辑 `config/model_config.yaml`：

```yaml
# RAG系统行为配置
rag:
  # 模式选择
  mode: "flexible"  # 'strict' 或 'flexible'
  
  # 置信度阈值配置（可选，高级用户）
  confidence:
    high_quality_min_docs: 3      # 高质量文档最少数量
    high_quality_min_score: 0.7   # 高质量文档最低相关性分数
    high_quality_confidence: 0.95 # 高质量答案的置信度
```

---

## 实际场景对比

### 场景 1：知识库相关性不足

**问题**：我的知识库中对"微服务架构"没有深入讨论

**灵活模式效果**：
```
[知识库] 根据我的笔记，微服务是一种架构模式...
[补充] 在实践中，常见的微服务框架包括...
[补充] 微服务的优点包括独立扩展、技术多样性...

置信度: 0.50 🟠 低
✅ 用户获得有用的信息，并知道置信度较低
```

**严格模式效果**：
```
知识库中相关信息不足

❌ 用户无法获得答案
```

---

### 场景 2：知识库包含相关内容

**问题**：关于"Python 装饰器"的问题

**灵活模式效果**：
```
[知识库] 根据我的 Python 笔记，装饰器用于修改函数行为...
[知识库] 常见的装饰器应用包括缓存、日志记录...
[补充] 此外，装饰器可以使用类实现...

置信度: 0.85 🟢 高
✅ 知识库完全覆盖问题，额外补充加深理解
```

**严格模式效果**：
```
[知识库] 根据我的 Python 笔记，装饰器用于修改函数行为...
[知识库] 常见的装饰器应用包括缓存、日志记录...

置信度: 0.85 🟢 高
✅ 相同的答案，但没有额外信息
```

---

## 选择建议

### 使用严格模式如果：
- ✅ 你的知识库很完善，覆盖了你的所有问题
- ✅ 你只想依赖自己的笔记
- ✅ 你需要符合监管要求
- ✅ 你在评估系统精确度

### 使用灵活模式如果：
- ✅ 你希望系统更有帮助（推荐默认选项）
- ✅ 知识库是参考资料，而非完整教科书
- ✅ 你想让系统"变得更聪明"
- ✅ 你关心答案的完整性而非严格性

**我们的建议**：🚀 **默认使用灵活模式**，这是设计的最佳选择

---

## 命令行用法

### 启动系统

```bash
# 默认灵活模式
uv run python main.py cli

# 或明确指定
uv run python -m src.cli --rag-mode flexible

# 严格模式
uv run python -m src.cli --rag-mode strict
```

### 在 CLI 中切换模式

虽然需要重启才能切换模式，但你可以：

1. **编辑配置文件** `config/model_config.yaml`，修改 `rag.mode`
2. **使用命令行参数** 覆盖配置

---

## 输出示例解读

### 灵活模式查询结果

```
查询: 如何在 Python 中使用异步编程？
------------------------------------------------------------

答案 (模型: qwen/qwen3.5-9b, RAG模式: 灵活模式（知识库优先+补充）, 置信度: 0.70 🟡 中等):
----------------------------------------
[知识库] 根据我的笔记，Python 使用 async/await...
[补充] 此外，asyncio 库提供了事件循环...

引用 (2 个):
----------------------------------------
1. Python异步编程 (分数: 0.74)
   预览: async 关键字用于定义协程函数...

检索统计: 2 个文档
```

**如何解读**：
- 🟡 中等置信度：答案有知识库支持，但信息不完整
- 2 个相关文档：检索到的上下文数量
- `[知识库]` 和 `[补充]`：明确标记答案来源

---

## 常见问题

**Q: 为什么有时候置信度是 0.5？**  
A: 这表示知识库没有找到高相关性的文档，但系统仍然基于先验知识提供了答案。这在灵活模式中是正常的，表示答案需要验证。

**Q: 能否实时切换模式？**  
A: 当前版本需要重启系统才能切换。可以编辑配置文件并重新启动。

**Q: 严格模式下，如果无法回答，会显示什么？**  
A: 系统会明确说"知识库中没有相关信息"，而不是提供可能不准确的答案。

**Q: 为什么有时候答案中有 `[补充]` 标记？**  
A: 这只在灵活模式下出现，表示该部分答案来自模型的先验知识，而非你的知识库。

---

## 相关配置

更多配置选项请查看 `config/model_config.yaml.example`。

主要相关配置：
- `rag.mode`：选择模式
- `rag.confidence.*`：置信度计算参数
- `llm.local/cloud`：LLM 模型设置
- `retrieval_enhancement`：检索优化设置
